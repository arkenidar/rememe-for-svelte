import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Extending the Game","description":"","frontmatter":{},"headers":[],"relativePath":"06-extending-the-game.md","filePath":"06-extending-the-game.md","lastUpdated":null}');
const _sfc_main = { name: "06-extending-the-game.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="extending-the-game" tabindex="-1">Extending the Game <a class="header-anchor" href="#extending-the-game" aria-label="Permalink to &quot;Extending the Game&quot;">​</a></h1><p>The best way to learn is to change things and see what happens. This page gives you guided exercises, each with a hint on where to start. Try them in any order.</p><h2 id="_1-change-the-grid-size" tabindex="-1">1. Change the grid size <a class="header-anchor" href="#_1-change-the-grid-size" aria-label="Permalink to &quot;1. Change the grid size&quot;">​</a></h2><p>The game currently uses 8 symbols (16 cards) in a 4×4 grid.</p><p><strong>Challenge:</strong> Make a 6×6 game (18 pairs) or a smaller 2×3 game for quick rounds.</p><p><strong>Hints:</strong></p><ul><li>Add more emoji to <code>EMOJIS</code> (or remove some).</li><li>Update <code>.grid</code> in the <code>&lt;style&gt;</code> block: change <code>repeat(4, 90px)</code> to <code>repeat(6, 90px)</code> (and adjust the mobile <code>@media</code> rule too).</li><li>The card count updates automatically because the deck is built from <code>[...EMOJIS, ...EMOJIS]</code>.</li></ul><h2 id="_2-add-a-timer" tabindex="-1">2. Add a timer <a class="header-anchor" href="#_2-add-a-timer" aria-label="Permalink to &quot;2. Add a timer&quot;">​</a></h2><p><strong>Challenge:</strong> Show how long the player took, and stop the timer when the game is won.</p><p><strong>Hints:</strong></p><ul><li>Add state: <code>let seconds = $state(0);</code> and a variable to hold the interval id.</li><li>Use <code>setInterval</code> in <code>newGame</code> and clear it when <code>matchedPairs</code> reaches <code>EMOJIS.length</code>.</li><li>Clean up the interval when the component unmounts with Svelte&#39;s <code>onDestroy</code>.</li></ul><h2 id="_3-track-a-best-score-with-localstorage" tabindex="-1">3. Track a best score with <code>localStorage</code> <a class="header-anchor" href="#_3-track-a-best-score-with-localstorage" aria-label="Permalink to &quot;3. Track a best score with \`localStorage\`&quot;">​</a></h2><p><strong>Challenge:</strong> Save the best (lowest) move count across sessions.</p><p><strong>Hints:</strong></p><ul><li>On win, compare <code>moves</code> to a stored best and update it if lower.</li><li>Use <code>localStorage.getItem(&#39;bestMoves&#39;)</code> and <code>localStorage.setItem(...)</code>.</li><li>Display the best score in the header.</li></ul><div class="tip custom-block"><p class="custom-block-title">What is localStorage?</p><p><code>localStorage</code> is a browser API that stores small string values per-site, persisting across page reloads. Values are always strings, so use <code>JSON.stringify</code> / <code>JSON.parse</code> or <code>Number()</code> when reading numbers.</p></div><h2 id="_4-add-more-visual-feedback" tabindex="-1">4. Add more visual feedback <a class="header-anchor" href="#_4-add-more-visual-feedback" aria-label="Permalink to &quot;4. Add more visual feedback&quot;">​</a></h2><p><strong>Challenge:</strong> Animate the card flip or add a &quot;shake&quot; effect on a mismatch.</p><p><strong>Hints:</strong></p><ul><li>Define extra classes (e.g. <code>.mismatch</code>) and apply them with a <code>class:</code> directive during the 800ms pause.</li><li>Use CSS <code>transform</code>, <code>transition</code>, and keyframe <code>@keyframes</code> animations.</li><li>Remember to remove the class when the cards flip back.</li></ul><h2 id="_5-prevent-flip-back-on-a-quick-double-click" tabindex="-1">5. Prevent flip-back on a quick double-click <a class="header-anchor" href="#_5-prevent-flip-back-on-a-quick-double-click" aria-label="Permalink to &quot;5. Prevent flip-back on a quick double-click&quot;">​</a></h2><p><strong>Challenge:</strong> Make the &quot;no match&quot; reveal last longer, or let the player click to dismiss it.</p><p><strong>Hints:</strong></p><ul><li>Change the <code>setTimeout</code> delay to a variable.</li><li>Clear the timeout if the player clicks a third card while <code>lock</code> is true (store the timeout id first).</li></ul><h2 id="_6-add-a-difficulty-selector" tabindex="-1">6. Add a difficulty selector <a class="header-anchor" href="#_6-add-a-difficulty-selector" aria-label="Permalink to &quot;6. Add a difficulty selector&quot;">​</a></h2><p><strong>Challenge:</strong> Let the player pick Easy (4×4), Medium (4×6), or Hard (6×6).</p><p><strong>Hints:</strong></p><ul><li>Extract the symbol list into a <code>DIFFICULTIES</code> object, each with its own symbols array and column count.</li><li>Store the chosen difficulty in <code>$state</code> and use it in <code>newGame</code>.</li><li>Write a small set of difficulty buttons with <code>onclick</code> handlers.</li></ul><h2 id="_7-restructure-into-smaller-components" tabindex="-1">7. Restructure into smaller components <a class="header-anchor" href="#_7-restructure-into-smaller-components" aria-label="Permalink to &quot;7. Restructure into smaller components&quot;">​</a></h2><p><strong>Challenge:</strong> Split the game into <code>Card.svelte</code>, <code>GameBoard.svelte</code>, and <code>Stats.svelte</code>.</p><p><strong>Hints:</strong></p><ul><li><strong>Props</strong> let a parent pass data into a child. In Svelte 5, declare them with <code>let { value } = $props();</code> in the child.</li><li>Use <strong>callback props</strong> (or Svelte 5 events) so a child <code>Card</code> can tell the parent it was clicked.</li><li>This is a great exercise for learning component boundaries and data flow.</li></ul><h2 id="suggested-first-steps" tabindex="-1">Suggested first steps <a class="header-anchor" href="#suggested-first-steps" aria-label="Permalink to &quot;Suggested first steps&quot;">​</a></h2><p>If this is your first modification, start with <strong>#1 (grid size)</strong> or <strong>#2 (timer)</strong> — they exercise exactly the reactivity and lifecycle concepts from <a href="/04-svelte-basics">Svelte Basics</a> with low risk.</p><p>Next: <a href="/07-glossary-and-links">Glossary &amp; Links</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("06-extending-the-game.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _06ExtendingTheGame = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _06ExtendingTheGame as default
};
